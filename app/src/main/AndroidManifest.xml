<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gom247.grandtoki">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Noaction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".DashBoardActivity"
            android:theme="@style/AppTheme.Noaction" />
        <activity
            android:name=".AccountActivity"
            android:theme="@style/AppTheme.Noaction" />
        <activity android:name=".LihatUserActivity" />
        <activity android:name=".UpdateUserActivity" />
        <activity android:name=".BarangActivity" />
        <activity android:name=".LihatBarangActivity" />
        <activity android:name=".UpdateBarangActivity" />
        <activity android:name=".KategoriActivity" />
        <activity android:name=".LihatBarangKategoriActivity"></activity>
    </application>

</manifest>